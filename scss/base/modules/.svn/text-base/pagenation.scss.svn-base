@charset "UTF-8";
/**
 * 分页控件 - 上一页、下一页和所有页码，请和php的pagenation widget、seajs的pagenation模块以及pagenation.scss一起使用
 * 
 *
 * @author: zhouhai
 * @create: 2014-07-14
 * @update: 2014-07-14
 */
@import "../variables";
@import "../reset";
@import "../mixins";
@import "../font";
@import "../base";

.pagenation{
	position: relative;
	font-size: $fontsize4;
	overflow: hidden;
	border-bottom: $thinBorder solid $lightBorderGray;
	margin-left:-$gap2;
	margin-right: -$gap2;
	span.prev,span.next{
		position: absolute;
		font-size: $fontsize9;
		color:$grayb;
		display: inline-block;
	}
	span.prev{
		top:$gap3;
		left:12px;
        @include transform(rotate(180deg));
	}
	span.next{
		top:$gap3;
		right:12px;
	}
	span.prev.disabled,span.next.disabled{
		color:$graye;
	}
	.ruler{
		margin:$gap1 $gap7;
		background: #e2e3e7;
		border-radius:$smallRadius;
		font-family: $numberFont;
		font-weight: bold;
		color:$gray2;
		box-shadow: inset 0 0 1px rgba(0,0,0,0.1);
		ul{
			overflow:hidden;
			padding:4px 0;
		}
		li{
			float: left;    			
			a{	display: inline-block;
				padding:1px 12px 0;
    			margin: 0 2px;
    			border-radius: $smallRadius;
    			line-height: 28px;
			}
			a.active, a:active{
    			background: #fafafc;
    			box-shadow: 0 2px 1px rgba(0,0,0,0.1);
    		}
		}
		
	}
}